{
  "name": "base",
  "private": true,
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp -r src/assets.json public/ && esbuild src/index.ts --bundle --minify --splitting --format=esm --outdir=public",
    "copy": "copy-and-watch src/assets.json public/",
    "start": "pnpm run dev",
    "dev": "pnpm run build && pnpm run copy && ws -p 3000 --spa index.html -z"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@mf-dev/host": "workspace:*",
    "local-web-server": "^5.3.0",
    "copy-and-watch": "^0.1.6",
    "concurrently": "^8.0.1",
    "esbuild": "^0.17.18"
  }
}
